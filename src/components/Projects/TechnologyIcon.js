import React from 'react';
import './ProjectMicroComponent.scss';

const TechnologyIcon = ({ elem }) => {
    return (
        <div className="technology-icon">{elem}</div>
      );
}
 
export default TechnologyIcon;